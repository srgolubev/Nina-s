// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Set `site` to the production URL once a domain is chosen, e.g.:
  // site: 'https://tranquilhavencoaching.com',
});
